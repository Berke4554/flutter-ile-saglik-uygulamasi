import 'package:flutter/material.dart';

class SporSayfasi extends StatelessWidget {
  const SporSayfasi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
